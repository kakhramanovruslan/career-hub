spring:
  application:
    name: review-service
  datasource:
    url: jdbc:postgresql://localhost:5437/review_service_db
    username: postgres
    password: o130805o
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: db/changelog/changelog-master.yml

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/